{
  "name": "form-collect",
  "version": "1.1.1",
  "description": "Collect form data following the WHATWG form submission algorithm",
  "main": "index.js",
  "module": "index.mjs",
  "scripts": {
    "test": "npm run build && jest",
    "rollup": "rollup",
    "build": "rollup index.mjs --file index.js --format cjs",
    "prepare": "npm run build"
  },
  "author": "Paul Kiddle",
  "license": "WTFPL",
  "devDependencies": {
    "jest": "^21.2.1",
    "rollup": "^0.64.1"
  }
}
